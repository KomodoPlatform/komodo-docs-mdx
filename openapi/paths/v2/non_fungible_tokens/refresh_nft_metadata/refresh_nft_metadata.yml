openapi: 3.0.0
info:
  title: Komodo DeFi Framework API - refresh_nft_metadata
  version: v2
  description: The refresh_nft_metadata method refreshes metadata for a specific NFT
    and related transactions using provided proxy URLs.
paths:
  /refresh_nft_metadata:
    post:
      summary: Refresh NFT Metadata
      description: The refresh_nft_metadata method refreshes metadata for a specific
        NFT and related transactions using provided proxy URLs.
      operationId: refresh_nft_metadata
      tags:
      - refresh_nft_metadata
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - refresh_nft_metadata
      responses:
        '200':
          description: Successful response for refresh_nft_metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/refresh_nft_metadata_Response'
              example:
                mmrpc: '2.0'
                result: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
