openapi: 3.0.0
info:
  title: Komodo DeFi Framework API - lightning::payments::send_payment
  version: v2
  description: Send a Lightning Network payment using the Komodo DeFi Framework API.
paths:
  /lightning::payments::send_payment:
    post:
      summary: Send Lightning Payment
      description: Send a Lightning Network payment using the Komodo DeFi Framework
        API.
      operationId: lightning::payments::send_payment
      tags:
      - lightning
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - lightning::payments::send_payment
      responses:
        '200':
          description: Successful response for lightning::payments::send_payment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/lightning_payments_send_payment_Response'
              example:
                mmrpc: '2.0'
                result:
                  payment_hash: string_value
                  status: string_value
                  id: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
