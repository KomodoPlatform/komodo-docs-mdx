openapi: 3.0.0
info:
  title: Komodo DeFi Framework API - lightning::payments::generate_invoice
  version: v2
  description: Generate a Lightning Network invoice using the Komodo DeFi Framework
    API.
paths:
  /lightning::payments::generate_invoice:
    post:
      summary: Generate Lightning Invoice
      description: Generate a Lightning Network invoice using the Komodo DeFi Framework
        API.
      operationId: lightning::payments::generate_invoice
      tags:
      - lightning
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - lightning::payments::generate_invoice
      responses:
        '200':
          description: Successful response for lightning::payments::generate_invoice
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/lightning_payments_generate_invoice_Response'
              example:
                mmrpc: '2.0'
                result:
                  invoice: string_value
                  id: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
