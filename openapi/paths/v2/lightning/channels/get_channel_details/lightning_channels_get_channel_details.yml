openapi: 3.0.0
info:
  title: Komodo DeFi Framework API - lightning::channels::get_channel_details
  version: v2
  description: Get details about a Lightning Network channel using the Komodo DeFi
    Framework API.
paths:
  /lightning::channels::get_channel_details:
    post:
      summary: Get Lightning Channel Details
      description: Get details about a Lightning Network channel using the Komodo
        DeFi Framework API.
      operationId: lightning::channels::get_channel_details
      tags:
      - lightning
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - lightning::channels::get_channel_details
      responses:
        '200':
          description: Successful response for lightning::channels::get_channel_details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/lightning_channels_get_channel_details_Response'
              example:
                mmrpc: '2.0'
                result:
                  status: string_value
                  details: {}
                  id: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
