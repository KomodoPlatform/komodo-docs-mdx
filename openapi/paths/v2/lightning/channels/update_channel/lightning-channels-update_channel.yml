openapi: 3.0.0
info:
  title: Komodo DeFi Framework API - lightning::channels::update_channel
  version: v2
  description: Update a Lightning Network channel using the Komodo DeFi Framework
    API.
paths:
  /lightning::channels::update_channel:
    post:
      summary: Update Lightning Channel
      description: Update a Lightning Network channel using the Komodo DeFi Framework
        API.
      operationId: lightning::channels::update_channel
      tags:
      - lightning
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - lightning::channels::update_channel
      responses:
        '200':
          description: Successful response for lightning::channels::update_channel
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/lightning_channels_update_channel_Response'
              example:
                mmrpc: '2.0'
                result:
                  channel_options: {}
                  id: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
