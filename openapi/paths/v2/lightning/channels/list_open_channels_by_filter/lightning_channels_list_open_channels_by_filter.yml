openapi: 3.0.0
info:
  title: Komodo DeFi Framework API - lightning::channels::list_open_channels_by_filter
  version: v2
  description: List open Lightning Network channels by filter using the Komodo DeFi
    Framework API.
paths:
  /lightning::channels::list_open_channels_by_filter:
    post:
      summary: List Open Lightning Channels by Filter
      description: List open Lightning Network channels by filter using the Komodo
        DeFi Framework API.
      operationId: lightning::channels::list_open_channels_by_filter
      tags:
      - lightning
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - lightning::channels::list_open_channels_by_filter
      responses:
        '200':
          description: Successful response for lightning::channels::list_open_channels_by_filter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/lightning_channels_list_open_channels_by_filter_Response'
              example:
                mmrpc: '2.0'
                result:
                  result: {}
                  id: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
