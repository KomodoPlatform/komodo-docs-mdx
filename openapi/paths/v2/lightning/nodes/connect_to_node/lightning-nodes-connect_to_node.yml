openapi: 3.0.0
info:
  title: Komodo DeFi Framework API - lightning::nodes::connect_to_node
  version: v2
  description: Connect to a Lightning node using the Komodo DeFi Framework API.
paths:
  /lightning::nodes::connect_to_node:
    post:
      summary: Connect to Lightning Node
      description: Connect to a Lightning node using the Komodo DeFi Framework API.
      operationId: lightning::nodes::connect_to_node
      tags:
      - lightning
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - lightning::nodes::connect_to_node
      responses:
        '200':
          description: Successful response for lightning::nodes::connect_to_node
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/lightning_nodes_connect_to_node_Response'
              example:
                mmrpc: '2.0'
                result:
                  result: string_value
                  id: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
