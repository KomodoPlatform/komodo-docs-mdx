openapi: 3.0.0
info:
  title: Komodo DeFi Framework API - swaps_and_orders
  version: v2
  description: This document provides an overview of all swap and order methods available
    in the Komodo DeFi Framework.
paths:
  /swaps_and_orders:
    post:
      summary: 'Komodo DeFi Framework Method Overview: Swaps and Orders'
      description: This document provides an overview of all swap and order methods
        available in the Komodo DeFi Framework.
      operationId: swaps_and_orders
      tags:
      - swaps_and_orders
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - swaps_and_orders
      responses:
        '200':
          description: Successful response for swaps_and_orders
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/swaps_and_orders_Response'
              example:
                mmrpc: '2.0'
                result: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
