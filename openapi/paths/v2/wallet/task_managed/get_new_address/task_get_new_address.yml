openapi: 3.0.0
info:
  title: Komodo DeFi Framework API - task::get_new_address
  version: v2
  description: Overview of get new address task-managed methods in the Komodo DeFi
    Framework API.
paths:
  /task::get_new_address:
    post:
      summary: Get New Address Task
      description: Overview of get new address task-managed methods in the Komodo
        DeFi Framework API.
      operationId: task::get_new_address
      tags:
      - task
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - task::get_new_address
      responses:
        '200':
          description: Successful response for task::get_new_address
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/task_get_new_address_Response'
              example:
                mmrpc: '2.0'
                result: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
