openapi: 3.0.0
info:
  title: Komodo DeFi Framework API - task::create_new_account
  version: v2
  description: Overview of create new account task-managed methods in the Komodo DeFi
    Framework API.
paths:
  /task::create_new_account:
    post:
      summary: Create New Account Task
      description: Overview of create new account task-managed methods in the Komodo
        DeFi Framework API.
      operationId: task::create_new_account
      tags:
      - task
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - task::create_new_account
      responses:
        '200':
          description: Successful response for task::create_new_account
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/task_create_new_account_Response'
              example:
                mmrpc: '2.0'
                result: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
