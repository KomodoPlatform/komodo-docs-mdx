openapi: 3.0.0
info:
  title: Komodo DeFi Framework API - task::enable_eth::init
  version: v2
  description: Task managed activation of ETH and EVM tokens.
paths:
  /task::enable_eth::init:
    post:
      summary: 'Enable ETH Task: Init'
      description: Task managed activation of ETH and EVM tokens.
      operationId: task::enable_eth::init
      tags:
      - task
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - task::enable_eth::init
      responses:
        '200':
          description: Successful response for task::enable_eth::init
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/task_enable_eth_init_Response'
              example:
                mmrpc: '2.0'
                result:
                  task_id: 123
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
