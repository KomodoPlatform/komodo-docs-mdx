openapi: 3.0.0
info:
  title: Komodo DeFi Framework - Active Swaps API
  version: v2
  description: OpenAPI specification for the active_swaps methods.
paths:
  /active_swaps:
    post:
      summary: Active Swaps
      description: The active_swaps method returns all the swaps that are currently
        running on the Komodo DeFi Framework API node.
      operationId: active_swaps
      tags:
      - active_swaps
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - active_swaps
                params:
                  type: object
                  properties:
                    include_status:
                      description: Whether to include swap statuses in response.
                      type: boolean
                      default: '`false`'
      responses:
        '200':
          description: Successful response for active_swaps
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/active_swaps_Response'
              example:
                mmrpc: '2.0'
                result:
                  uuids: string_value
                  statuses: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  schemas:
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        error_path:
          type: string
        error_trace:
          type: string
        error_type:
          type: string
        error_data:
          type: object
