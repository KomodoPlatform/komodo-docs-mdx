export const title = "Komodo DeFi Framework Method: Show Priv Key";
export const description = "The show_priv_key method returns the private key of the specified coin in a format compatible with coin wallets.";

# Show Priv Key

## show\_priv\_key {{label: 'show_priv_key', tag: 'API-v1'}}

The `show_priv_key` method returns the private key of the specified coin in a format compatible with coin wallets. The output can be used for the `importprivkey` method (UTXO coins) or as a private key for MyEtherWallet (ETH/ERC20).

### Request arguments

| Parameter | Type   | Required | Description                                     |
| --------- | ------ | :------: | ----------------------------------------------- |
| coin      | string |     ✓    | The name of the coin of the private key to show |

### Response parameters

| Parameter | Type   | Description                 |
| --------- | ------ | --------------------------- |
| coin      | string | The name of the coin        |
| priv\_key | string | The private key of the coin |

#### 📌 Example

#### Request

<CodeGroup title="show_priv_key" tag="POST" label="show_priv_key" mm2MethodDecorate="true">
  ```json
  {
    "userpass": "RPC_UserP@SSW0RD",
    "method": "show_priv_key",
    "coin": "HELLOWORLD"
  }
  ```
</CodeGroup>

<CollapsibleSection expandedText="Hide Response" collapsedText="Show Response">
  #### Response (UTXO WIF)

  ```json
  {
      "coin": "HELLOWORLD",
      "priv_key": "UvCjJf4dKSs2vFGVtCnUTAhR5FTZGdg43DDRa9s7s5DV1sSDX14g"
  }
  ```

  #### Response (0x-prefixed ETH private key)

  ```json
  {
      "coin": "ETH",
      "priv_key": "0xb8c774f071de08c7fd8f62b97f1a5726f6ce9f1bcf141b70b86689254ed6714e"
  }
  ```
</CollapsibleSection>
