export const title = "Komodo DeFi Framework Method: Validate Address";
export const description = "The validateaddress method checks if an input string is a valid address of the specified coin.";

# validateaddress

**validateaddress coin address**

The `validateaddress` method checks if an input string is a valid address of the specified coin.

## Arguments

<CompactTable source="legacy/wallet.ValidateAddressRequest" />

## Response

<CompactTable source="legacy/wallet.ValidateAddressResponse" />

#### 📌 Examples

<CodeGroup title="" tag="POST" label="validateaddress" mm2MethodDecorate="true" source="legacy/wallet.ValidateAddress" />

<KdfResponses responseKey="legacy/wallet.ValidateAddress" />
