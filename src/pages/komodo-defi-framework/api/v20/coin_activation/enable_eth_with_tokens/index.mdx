export const title = "Komodo DeFi Framework Method: Enable ETH with Tokens";
export const description = "Learn how enable_eth_with_tokens activates a platform coin like ETH along with multiple ERC20 tokens in a single API command";

# enable\_eth\_with\_tokens

The Komodo DeFi Framework supports ETH(Ethereum) and many other EVM type platform coins like AVAX(Avalanche), BNB(Binance), FTM(Fantom), MATIC(Polygon), ONE(Harmony), ETH-ARB20(Arbitrum).
Additionally, it supports ERC20 tokens on the ETH chain and associated ERC20 like tokens on the rest of the platform coin chains.Using this method, you can enable a platform coin along with multiple ERC20 like tokens of the platform coin chain in a single command.

### Request Parameters

<CompactTable source="v2/coin_activation.EnableEthWithTokensArguments" />

<Note>
  When running in HD mode, do not use the `nft_req` object paramater when activating your NFT network coins. Instead, use the [enable\_nft](/komodo-defi-framework/api/v20/non_fungible_tokens/enable_nft/) method after activating.
</Note>

### Response Parameters

<CompactTable source="v2/coin_activation.EnableEthWithTokensResponse" />

### 📌 Examples

#### Request including optional gas station parameters

<CodeGroup title="" tag="POST" label="enable_eth_with_tokens" mm2MethodDecorate="true" source="v2/coin_activation.EnableEthWithTokensGasStation" />

<KdfResponses responseKey="v2/coin_activation.EnableEthWithTokensGasStation" />

#### Request with `get_balances` set to false

<CodeGroup title="" tag="POST" label="enable_eth_with_tokens" mm2MethodDecorate="true" source="v2/coin_activation.EnableEthWithTokensMaticBalancesFalse" />

<KdfResponses responseKey="v2/coin_activation.EnableEthWithTokensMaticBalancesFalse" />

#### Request including NFT initialization

<CodeGroup title="" tag="POST" label="enable_eth_with_tokens" mm2MethodDecorate="true" source="v2/coin_activation.EnableEthWithTokensMaticNft" />

<KdfResponses responseKey="v2/coin_activation.EnableEthWithTokensMaticNft" />

#### Request using WalletConnect

Prior to coin activation using WalletConnect, you need to establish a connection with [wc\_new\_connection](/komodo-defi-framework/api/v20-dev/wc_new_connection/#wc-new-connection)

<CodeGroup title="" tag="POST" label="enable_eth_with_tokens" mm2MethodDecorate="true" source="v2/coin_activation.EnableEthWithTokensWalletConnect" />

<KdfResponses responseKey="v2/coin_activation.EnableEthWithTokensWalletConnect" />

### Error Types

<CompactTable source="v2/coin_activation.EnableEthWithTokensErrorTypes" />
