export const title = "AtomicDEX Method: Remove Node from Version Stat";
export const description = "The remove_node_from_version_stat method removes a Node from the local database which tracks which version of MM2 it is running.";

# remove_node_from_version_stat

The `remove_node_from_version_stat` method removes a Node (by name) from the local database which tracks which version of MM2 it is running. The name parameter is an arbitrary identifying string, such as "seed_alpha" or "dragonhound_DEV".

## Arguments

| Structure | Type   | Description                   |
| --------- | ------ | ----------------------------- |
| name      | string | the name assigned to the node |

#### 📌 Examples

#### Command

<CodeGroup title="" tag="POST" label="remove_node_from_version_stat">
```json {{ mm2MethodDecorate : true }}
{
  "mmrpc": "2.0",
  "method": "remove_node_from_version_stat",
  "userpass": "MM2_RPC_PASSWORD",
  "params": {
    "name": "dragonhound_DEV"
  }
}
```
</CodeGroup>


<CollapsibleSection expandedText='Hide Response' collapsedText='Show Response'>
#### Response (success)

```json
{
  "mmrpc": "2.0",
  "result": "success",
  "id": null
}
```

</CollapsibleSection>
