openapi: 3.0.0
info:
  title: Komodo DeFi Framework - Clear Nft Db V2 API
  version: v2
  description: OpenAPI specification for the clear_nft_db_v2 methods.
paths:
  /clear_nft_db:
    post:
      summary: Clear NFT Database
      description: Method description for clear_nft_db
      operationId: clear_nft_db
      tags:
      - clear_nft_db
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - clear_nft_db
      responses:
        '200':
          description: Successful response for clear_nft_db
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/clear_nft_db_Response'
              example:
                mmrpc: '2.0'
                result: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  schemas:
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        error_path:
          type: string
        error_trace:
          type: string
        error_type:
          type: string
        error_data:
          type: object
