openapi: 3.0.0
info:
  title: Komodo DeFi Framework - Refresh Nft Metadata V2 API
  version: v2
  description: OpenAPI specification for the refresh_nft_metadata_v2 methods.
paths:
  /refresh_nft_metadata:
    post:
      summary: Refresh NFT Metadata
      description: The refresh_nft_metadata method refreshes metadata for a specific
        NFT and related transactions using provided proxy URLs.
      operationId: refresh_nft_metadata
      tags:
      - refresh_nft_metadata
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - refresh_nft_metadata
      responses:
        '200':
          description: Successful response for refresh_nft_metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/refresh_nft_metadata_Response'
              example:
                mmrpc: '2.0'
                result: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  schemas:
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        error_path:
          type: string
        error_trace:
          type: string
        error_type:
          type: string
        error_data:
          type: object
