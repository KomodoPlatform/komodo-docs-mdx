openapi: 3.0.0
info:
  title: Komodo DeFi Framework - Fee API
  version: v2
  description: OpenAPI specification for the Fee methods.
paths:
  /Fee:
    post:
      summary: 'Komodo DeFi Framework Overview: EVM Fee Management'
      description: Method description for Fee
      operationId: Fee
      tags:
      - Fee
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - Fee
      responses:
        '200':
          description: Successful response for Fee
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fee_Response'
              example:
                mmrpc: '2.0'
                result: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  schemas:
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        error_path:
          type: string
        error_trace:
          type: string
        error_type:
          type: string
        error_data:
          type: object
