openapi: 3.0.0
info:
  title: Komodo DeFi Framework - Coin Activation API
  version: v2
  description: OpenAPI specification for the coin_activation methods.
paths:
  /coin_activation:
    post:
      summary: 'Komodo DeFi Framework Method Overview: Coin Activation'
      description: Method description for coin_activation
      operationId: coin_activation
      tags:
      - coin_activation
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - coin_activation
      responses:
        '200':
          description: Successful response for coin_activation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/coin_activation_Response'
              example:
                mmrpc: '2.0'
                result: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  schemas:
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        error_path:
          type: string
        error_trace:
          type: string
        error_type:
          type: string
        error_data:
          type: object
