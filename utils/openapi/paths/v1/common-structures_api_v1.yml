openapi: 3.0.0
info:
  title: Komodo DeFi Framework - Common-Structures API
  version: v1
  description: OpenAPI specification for the common-structures methods.
paths:
  /common-structures:
    post:
      summary: Common Structures
      description: Starting with version beta-2.1.3434, the Komodo DeFi SDK supports
        the standardized protocol format called mmrpc 2.0.
      operationId: common-structures
      tags:
      - common-structures
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - common-structures
      responses:
        '200':
          description: Successful response for common-structures
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/common-structures_Response'
              example:
                mmrpc: '2.0'
                result: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  schemas:
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        error_path:
          type: string
        error_trace:
          type: string
        error_type:
          type: string
        error_data:
          type: object
