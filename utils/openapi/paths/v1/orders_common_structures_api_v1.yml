openapi: 3.0.0
info:
  title: Komodo DeFi Framework - Orders Common Structures API
  version: v1
  description: OpenAPI specification for the orders_common_structures methods.
paths:
  /orders_common_structures:
    post:
      summary: 'Komodo DeFi SDK Common Structures: Orders'
      description: Each order on the Komodo Defi oderbook can be queried to view full
        details of each order for a pair, or the best orders for a ticker.
      operationId: orders_common_structures
      tags:
      - orders_common_structures
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - orders_common_structures
      responses:
        '200':
          description: Successful response for orders_common_structures
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/orders_common_structures_Response'
              example:
                mmrpc: '2.0'
                result: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  schemas:
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        error_path:
          type: string
        error_trace:
          type: string
        error_type:
          type: string
        error_data:
          type: object
