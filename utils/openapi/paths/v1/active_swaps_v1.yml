openapi: 3.0.0
info:
  title: Komodo DeFi Framework - Active Swaps V1 API
  version: v1
  description: OpenAPI specification for the active_swaps_v1 methods.
paths:
  /active_swaps:
    post:
      summary: Active Swaps
      description: Method description for active_swaps
      operationId: active_swaps
      tags:
      - active_swaps
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - active_swaps
      responses:
        '200':
          description: Successful response for active_swaps
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/active_swaps_Response'
              example:
                mmrpc: '2.0'
                result:
                  statuses: {}
                  uuids: string_value
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  schemas:
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        error_path:
          type: string
        error_trace:
          type: string
        error_type:
          type: string
        error_data:
          type: object
