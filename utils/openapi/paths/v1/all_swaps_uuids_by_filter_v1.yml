openapi: 3.0.0
info:
  title: Komodo DeFi Framework - All Swaps Uuids By Filter V1 API
  version: v1
  description: OpenAPI specification for the all_swaps_uuids_by_filter_v1 methods.
paths:
  /all_swaps_uuids_by_filter:
    post:
      summary: All Swaps UUIDs by Filter
      description: The all_swaps_uuids_by_filter method returns all uuids of swaps
        that match the selected filters.
      operationId: all_swaps_uuids_by_filter
      tags:
      - all_swaps_uuids_by_filter
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - all_swaps_uuids_by_filter
      responses:
        '200':
          description: Successful response for all_swaps_uuids_by_filter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/all_swaps_uuids_by_filter_Response'
              example:
                mmrpc: '2.0'
                result:
                  found_records: {}
                  from_timestamp: {}
                  my_coin: string_value
                  other_coin: string_value
                  records_found: {}
                  to_timestamp: {}
                  uuids: string_value
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  schemas:
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        error_path:
          type: string
        error_trace:
          type: string
        error_type:
          type: string
        error_data:
          type: object
