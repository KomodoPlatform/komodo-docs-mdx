openapi: 3.0.0
info:
  title: Komodo DeFi Framework - Rational Number Type API
  version: v1
  description: OpenAPI specification for the rational_number_type methods.
paths:
  /rational_number_type:
    post:
      summary: Rational Number Type
      description: The Komodo DeFi Framework API now offers the num-rational crate
        feature. This is used to represent order volumes and prices.
      operationId: rational_number_type
      tags:
      - rational_number_type
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - userpass
              - method
              properties:
                userpass:
                  type: string
                  description: User password for authentication.
                  example: RPC_UserP@SSW0RD
                method:
                  type: string
                  description: Name of the method to be called.
                  enum:
                  - rational_number_type
      responses:
        '200':
          description: Successful response for rational_number_type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/rational_number_type_Response'
              example:
                mmrpc: '2.0'
                result: {}
                id: 0
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  schemas:
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        error_path:
          type: string
        error_trace:
          type: string
        error_type:
          type: string
        error_data:
          type: object
