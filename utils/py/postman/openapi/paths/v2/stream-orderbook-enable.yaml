/api/v2/method:
  post:
    operationId: stream-orderbook-enable
    summary: 'Komodo DeFi Framework Method: stream-orderbook-enable'
    description: 'TODO: Add description for stream-orderbook-enable method'
    tags:
    - Komodo DeFi Framework API V2
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            properties:
              method:
                type: string
                enum:
                - stream-orderbook-enable
                description: The method name
              mmrpc:
                type: string
                enum:
                - '2.0'
                description: API version
            required:
            - method
            - mmrpc
    responses:
      '200':
        description: Success
      '400':
        description: Bad request
      '500':
        description: Internal server error
