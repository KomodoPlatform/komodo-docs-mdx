# OpenAPI path spec for get_directly_connected_peers (v1)
/api/v1/get_directly_connected_peers:
  post:
    operationId: get_directly_connected_peers
    summary: Komodo DeFi Framework Method: Get Peers Info
    description: The get_directly_connected_peers method returns an array of peers (with their multiaddresses) currently connected to the requesting node.
    x-mdx-doc-path: ../../../../src/pages/komodo-defi-framework/api/legacy/get_directly_connected_peers/index.mdx
    requestBody:
      required: true
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../../components/schemas/Common.yaml#/RpcV1Request'
              - type: object
                properties:
                  method:
                    type: string
                    enum: [get_directly_connected_peers]
                    description: Method name (must be 'get_directly_connected_peers').
    responses:
      '200':
        description: Success
        content:
          application/json:
            schema:
              type: object
              properties:
                result:
                  type: object
                  additionalProperties:
                    type: array
                    items:
                      type: string
                  description: All connected peers with their multiaddresses.
              required:
                - result
      '400':
        description: Bad request
        content:
          application/json:
            schema:
              $ref: '../../../components/schemas/Common.yaml#/RpcErrorResponse'
      '500':
        description: Internal server error
        content:
          application/json:
            schema:
              $ref: '../../../components/schemas/Common.yaml#/RpcErrorResponse'
    # TODO: Use $ref to schemas if/when available in components/schemas 