# OpenAPI path spec for list_banned_pubkeys (v1)
/api/v1/list_banned_pubkeys:
  post:
    operationId: list_banned_pubkeys
    summary: Komodo DeFi Framework Method: List Banned Pubkeys
    description: The list_banned_pubkeys method returns a list of public keys of nodes that are banned from interacting with the node executing the method.
    x-mdx-doc-path: ../../../../src/pages/komodo-defi-framework/api/legacy/list_banned_pubkeys/index.mdx
    requestBody:
      required: true
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../../components/schemas/Common.yaml#/RpcV1Request'
              - type: object
                properties:
                  method:
                    type: string
                    enum: [list_banned_pubkeys]
                    description: Method name (must be 'list_banned_pubkeys').
    responses:
      '200':
        description: Success
        content:
          application/json:
            schema:
              type: object
              properties:
                result:
                  type: object
                  additionalProperties:
                    type: object
                    properties:
                      type:
                        type: string
                        enum: ["Manual", "FailedSwap"]
                        description: The type of the ban; possible values: Manual or FailedSwap.
                      caused_by_swap:
                        type: string
                        format: uuid
                        description: The uuid of the swap that triggered the ban; present only for the FailedSwap type.
                      caused_by_event:
                        type: object
                        description: The swap event that triggered the ban; present only for the FailedSwap type.
                      reason:
                        type: string
                        description: The reason for the Manual ban.
                    required:
                      - type
                  description: The list of pubkeys banned by current node.
              required:
                - result
      '400':
        description: Bad request
        content:
          application/json:
            schema:
              $ref: '../../../components/schemas/Common.yaml#/RpcErrorResponse'
      '500':
        description: Internal server error
        content:
          application/json:
            schema:
              $ref: '../../../components/schemas/Common.yaml#/RpcErrorResponse'
    # TODO: Use $ref to schemas if/when available in components/schemas 