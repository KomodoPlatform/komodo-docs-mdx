# OpenAPI path spec for active_swaps (v1)
/api/v1/active_swaps:
  post:
    operationId: active_swaps
    summary: "Komodo DeFi Framework Method: Active Swaps"
    description: The active_swaps method returns all the swaps that are currently running on the Komodo DeFi Framework API node.\n\nThis method has been deprecated in favor of active_swaps v2. The legacy version does not include v2 (Trading Protocol Upgrade) swap statuses in responses (it only returns uuids for these).
    x-mdx-doc-path: ../../../../src/pages/komodo-defi-framework/api/legacy/active_swaps/index.mdx
    parameters:
      - name: include_status
        in: query
        description: Optional, defaults to false. Whether to include swap statuses in response.
        required: false
        schema:
          type: boolean
    requestBody:
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../../components/schemas/Common.yaml#/RpcV1Request'
              - type: object
                properties:
                  method:
                    type: string
                    enum: [active_swaps]
                    description: Method name
                  include_status:
                    type: boolean
                    description: Optional, defaults to false. Whether to include swap statuses in response.
    responses:
      '200':
        description: Success
        content:
          application/json:
            schema:
              type: object
              properties:
                result:
                  type: object
                  properties:
                    uuids:
                      type: array
                      items:
                        type: string
                        format: uuid
                      description: A list of currently active swap UUIDs.
                    statuses:
                      type: object
                      additionalProperties:
                        $ref: ../../components/schemas/Swaps.yaml#/SwapStatus
                      description: Only visible if include_status request parameter is true. A map of standard SwapStatus objects, delineated by the related UUID.
              required:
                - result
      '400':
        description: Bad request
        content:
          application/json:
            schema:
              $ref: '../../../components/schemas/Common.yaml#/RpcErrorResponse'
      '500':
        description: Internal server error
        content:
          application/json:
            schema:
              $ref: '../../../components/schemas/Common.yaml#/RpcErrorResponse'
    # TODO: Use $ref to schemas if/when available in components/schemas 
    # TODO: Review if all parameter and response details are complete and correct. 