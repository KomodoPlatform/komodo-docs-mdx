# OpenAPI path spec for orderbook_depth (v1)
/orderbook_depth:
  post:
    operationId: orderbook_depth
    summary: "Komodo DeFi Framework Method: Orderbook Depth"
    description: The orderbook_depth method returns the number of asks and bids for the specified trading pairs.
    x-mdx-doc-path: ../../../../src/pages/komodo-defi-framework/api/legacy/orderbook_depth/index.mdx
    requestBody:
      required: true
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../../components/schemas/Common.yaml#/RpcV1Request'
              - type: object
                properties:
                  method:
                    type: string
                    enum: [orderbook_depth]
                    description: Method name (must be 'orderbook_depth').
                  pairs:
                    type: array
                    items:
                      type: array
                      items:
                        $ref: ../../../components/schemas/Common.yaml#/CoinTicker
                      minItems: 2
                      maxItems: 2
                    description: An array of trading pairs (each pair is an array of 2 coin tickers).
                required:
                  - pairs
    responses:
      '200':
        description: Success
        content:
          application/json:
            schema:
              type: object
              properties:
                result:
                  type: array
                  items:
                    $ref: ../../components/schemas/Orders.yaml#/PairDepth
                  description: An array of pair depth objects.
              required:
                - result
      '400':
        description: Bad request
        content:
          application/json:
            schema:
              $ref: '../../../components/schemas/Common.yaml#/RpcErrorResponse'
      '500':
        description: Internal server error
        content:
          application/json:
            schema:
              $ref: '../../../components/schemas/Common.yaml#/RpcErrorResponse' 