# OpenAPI path spec for get_gossip_mesh (v1)
/api/v1/get_gossip_mesh:
  post:
    operationId: get_gossip_mesh
    summary: Komodo DeFi Framework Method: Get Gossip Mesh
    description: The get_gossip_mesh method returns an array of peerIDs added to a topics' mesh for each known gossipsub topic.
    x-mdx-doc-path: ../../../../src/pages/komodo-defi-framework/api/legacy/get_gossip_mesh/index.mdx
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            properties:
              userpass:
                type: string
                description: RPC authentication password.
              method:
                type: string
                enum: [get_gossip_mesh]
                description: Method name (must be 'get_gossip_mesh').
            required:
              - userpass
              - method
    responses:
      '200':
        description: Success
        content:
          application/json:
            schema:
              type: object
              properties:
                result:
                  type: object
                  additionalProperties:
                    type: array
                    items:
                      type: string
                  description: PeerIDs added to a topics' mesh (for each known gossipsub topic).
              required:
                - result
      '400':
        description: Bad request
      '500':
        description: Internal server error
    # TODO: Use $ref to schemas if/when available in components/schemas 