post:
  operationId: "lightning_channels_list_closed_channels_by_filter"
  tags:
    - Lightning Channels
  summary: List closed Lightning channels by filter
  description: Returns a list of closed channels filtered by the provided filter object.
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            coin:
              type: string
              description: The ticker of the coin
            filter:
              type: object
              description: Optional. Filter object
            paging_options:
              type: object
              description: Optional. Pagination options
            limit:
              type: integer
              description: Optional. Max number of results
  responses:
    '200':
      description: List of closed channels
      content:
        application/json:
          schema:
            type: object
            properties:
              closed_channels:
                type: array
                items:
                  type: object 