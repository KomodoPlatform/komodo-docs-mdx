/task-init_trezor-cancel:
  post:
    operationId: task_init_trezor_cancel
    summary: Cancel Trezor Initialization Task
    description: |
      Cancel a Trezor hardware wallet initialization task.
    x-mdx-doc-path: ../../src/pages/komodo-defi-framework/api/v20/utils/task_init_trezor/index.mdx
    requestBody:
      required: true
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../../components/schemas/Common.yaml#/RpcV2Request'
              - type: object
                properties:
                  method:
                    type: string
                    enum: ["task::init_trezor::cancel"]
                    description: The method name
                    example: "task::init_trezor::cancel"
                  params:
                    type: object
                    required:
                      - task_id
                    properties:
                      task_id:
                        type: integer
                        description: The identifying number returned when initiating the task
                        example: 1
    responses:
      '200':
        description: Successfully cancelled Trezor initialization task
        content:
          application/json:
            schema:
              allOf:
                - $ref: '../../../components/schemas/Common.yaml#/RpcSuccessResponse'
                - type: object
                  properties:
                    result:
                      type: string
                      description: Result of the cancellation
                      enum: ["success"]
                      example: "success"
      '400':
        description: Bad request
        content:
          application/json:
            schema:
              allOf:
                - $ref: '../../../components/schemas/Common.yaml#/RpcErrorResponse'
                - type: object
                  properties:
                    error_type:
                      type: string
                      description: Type of error
                      enum:
                        - "TaskNotFound"
                        - "InvalidParam"
      '500':
        description: Internal server error
        content:
          application/json:
            schema:
              $ref: '../../../components/schemas/Common.yaml#/RpcErrorResponse' 